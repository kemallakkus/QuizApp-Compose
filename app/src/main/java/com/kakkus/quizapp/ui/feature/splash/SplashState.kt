package com.kakkus.quizapp.ui.feature.splash

import androidx.compose.runtime.Immutable

@Immutable
data class SplashState(
    val isLoading: Boolean = true,
    // Başka değişkenler de eklenebilir.
)